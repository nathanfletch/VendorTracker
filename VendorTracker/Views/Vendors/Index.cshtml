@using VendorTracker.Models;
@{
  Layout = "_Layout";
}

@if(Model.Count == 0)
{
  <p>You're not tracking any vendors yet.</p>
}

<ul>
  @foreach(Vendor vendor in Model)
  {
    <li>@vendor.Name</li>
  }
</ul>

<a href="/vendors/new">Track a vendor</a>